{
  "name": "ai-pentest-copilot",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:contracts": "npm run build --workspace=packages/contracts",
    "start:server": "npm run start --workspace=packages/server"
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
